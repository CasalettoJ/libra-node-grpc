{
  "name": "libra_nodejs_grpc",
  "version": "0.0.1",
  "description": "Test gRPC Nodejs project w/ Libra protobufs",
  "main": "index.js",
  "author": "Jake Casaletto",
  "license": "MIT",
  "scripts": {
    "test-libra-env-var": "if [ -z \"$LIBRA\" ]; then echo \"\\$LIBRA environment variable missing.\"; echo \"Please set \\$LIBRA to the Libra Core root path\"; exit 1; fi",
    "testnet": "npm run test-libra-env-var && (cd $LIBRA && ./scripts/cli/start_cli_testnet.sh)",
    "setup": "npm install && mkdir dist && npm run clean",
    "clean": "rm -rf dist/* && mkdir dist/proto && npm run copyprotos",
    "copyprotos": "npm run test-libra-env-var && echo \"Copying Libra protobuf files...\"; find \"$LIBRA\" -name \"*.proto\" -exec cp {} dist/proto/ \\;"
  },
  "dependencies": {
    "@grpc/proto-loader": "^0.5.1",
    "grpc": "^1.21.1"
  },
  "devDependencies": {
    "eslint": "^6.0.0",
    "eslint-config-prettier": "^5.0.0",
    "eslint-plugin-flowtype": "^3.10.5",
    "eslint-plugin-prettier": "^3.1.0",
    "flow-bin": "^0.101.1",
    "prettier": "^1.18.2"
  }
}